%h2 Accept Bylaws

.bylaws
  = render partial: 'shared/bylaws'

= semantic_form_for @user do |f|
  = render partial: 'accept_bylaws_form', locals: {f: f}
  = f.submit 'Update', class: 'btn btn-sm btn-primary btn-with-padding'
