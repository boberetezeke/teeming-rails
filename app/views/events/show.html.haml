%h3 Event
= render partial: 'events/show', locals: { event: @event, show_description: true, show_stats: true, show_rsvp: false, event_rsvp: @event_rsvp }
%h3 Messages
- if @event.messages
  = render partial: 'messages/table', locals: { messages: @event.messages }