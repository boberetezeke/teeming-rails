- if member_groups.present?
  %table.table
    %tr
      %th Name
      %th Created
    - member_groups.each do |member_group|
      %tr
        %td= link_to(member_group.name, member_group)
        %td= member_group.created_at.strftime("%m/%d/%Y")
- else
  %p There are no member_groups yet
